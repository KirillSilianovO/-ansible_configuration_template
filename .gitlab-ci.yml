stages:
  - lint
  - test

lint:
  stage: lint
  image: kirillsilianov/ansible-linter:latest
  before_script:
    - cp ./ansible-lint.yaml /config/ansible-lint.yaml
  script:
    - ansible-lint -c /config/ansible-lint.yaml

sast:
  stage: test
include:
  - template: Security/SAST.gitlab-ci.yml
